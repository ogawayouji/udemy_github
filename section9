タグ
  コミットにタグをつける　探しやすくなる

git tag
  タグ一覧を表示する
  git tag -l <>
    タグのパターン絞り込み

注釈付き
  git tag -a <> -m <>
軽量版
  git tag <>
  git tag <> コミット名
  後からタグ付け

git show <>
  タグのデータを表示する

git push <リモート> <タグ>
  タグをリモートリポジトリに送信する
  git push origin --tags
  タグを一斉送信